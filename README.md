# Draftio Docs

Mintlify documentation for Draftio / StarFusion.

## Structure

- `docs.json` is the Mintlify project config.
- `*.mdx` files contain documentation pages.
- `images/` and `videos/` contain documentation media assets.

## Local Preview

```bash
mint dev
```
